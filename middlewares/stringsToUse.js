const STRING_BASE = 'o campo {#label} tem que ser uma string';
const STRING_EMPTY = 'o campo {#label} não pode estar vazio';
const STRING_MIN = 'o campo {#label} tem que ter no mínimo {#limit} caracteres';
const STRING_MAX = 'o campo {#label} tem que ter no máximo {#limit} caracteres';

module.exports = {
  STRING_BASE,
  STRING_EMPTY,
  STRING_MAX,
  STRING_MIN,
};